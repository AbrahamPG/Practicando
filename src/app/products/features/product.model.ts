export interface Product {
    id: number;
    nombre: string;
    category: string;
    price: number | string;
    stock: number;
    image: string;  // Asegúrate de que el nombre coincida con la API
  }
  